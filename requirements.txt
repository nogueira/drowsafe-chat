# DrowSAFE Python Dependencies
# Install via: pip install -r requirements.txt
# Note: opencv-python is installed at system level on Raspberry Pi OS
# (python3-opencv via apt). The venv uses --system-site-packages to access it.

# On desktop Python 3.12, newer MediaPipe releases removed mp.solutions,
# which this project uses for FaceMesh. Keep desktop installs on 0.10.21.
# On Raspberry Pi OS aarch64, keep MediaPipe unconstrained because PyPI
# aarch64 wheels may not carry normal version metadata.
mediapipe==0.10.21; platform_machine != "aarch64"
mediapipe; platform_machine == "aarch64"
onnxruntime>=1.17.0
pygame>=2.5.0
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
Pillow>=10.0.0
psutil>=5.9.0
pytest>=8.0.0
opencv-contrib-python==4.11.0.86; platform_system == "Windows"
# lgpio is the correct GPIO library for Raspberry Pi 5 on Bookworm
# (RPi.GPIO is not supported on Pi 5)
lgpio; platform_system == "Linux"
